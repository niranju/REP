#include<stdio.h>
int main()
{
char c;
printf("enter the character");
if("%c",&c);
if((c>='a'&&c<='z')||(c>='A'&&c<='Z'))
printf("%c is not an alphabet");
else
printf("%c is not an alphabet");
return 0;
}
